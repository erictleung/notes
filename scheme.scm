; Scheme Notes

; one kind of interpreter
csi ; chicken interpreter

; compile file with interpreter
csc hello.scm ; chicken compiler

; simple arithmetic
(+ 1 2 3 4) ; 10
(- 1 2 3 4) ; 8

; set variable pi
(define pi 3.14159)

set compound procedure/function
(define (square x) (* x x))
